FrontendWebBundle_web_index:
    pattern:  /
    defaults: { _controller: FrontendWebBundle:Home:index }

FrontendWebBundle_search_result:
    pattern:  /results
    defaults: { _controller: FrontendWebBundle:Search:results }

FrontendWebBundle_search_result_ajax:
    pattern:  /results/ajax
    defaults: { _controller: FrontendWebBundle:Search:resultsAjax }

FrontendWebBundle_client_subscribe:
  pattern:  /subscribe
  defaults: { _controller: FrontendWebBundle:Client:subscribe }
  
FrontendWebBundle_client_subscribe_success:
  pattern:  /subscribe/success
  defaults: { _controller: FrontendWebBundle:Client:subscribeSuccess }  

FrontendWebBundle_vols_store:
  pattern:  /vols
  defaults: { _controller: FrontendWebBundle:Vols:store }
    